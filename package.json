{
  "name": "opportunity-flops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.mjs",
    "build": "node --check public/app.js && node --check public/model.js && node --check public/permalink.js && node --check public/scale.js",
    "test": "node --test"
  }
}
